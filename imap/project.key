\javaSource ".";
/*
\predicates {
  isIntMap(Map);
}

\rules {
  def_isIntMap {
    \schemaVar \term Map m;
    \schemaVar \variable any av;

    \find(isIntMap(m))
    \varcond(\notFreeIn(av, m))
    \replacewith(\forall av; (inDomain(m, av) -> int::instance(av) = TRUE)
               & \forall av; (mapGet(m, av) = mapUndef | int::instance(mapGet(m, av)) = TRUE) )
  };
}
*/
\chooseContract
